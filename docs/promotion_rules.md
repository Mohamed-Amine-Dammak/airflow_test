# Promotion Rules

- `logic_change`: must pass hard gates and beat or match champion.
- `config_only`: valid config + minimum threshold is enough.
- `destructive_change`: stricter safety thresholds and approvals.
