# customer_sales Pipeline

This pipeline demonstrates:

- Versioned DAG logic in `versions/`
- Metadata lifecycle in `manifest/manifest.json`
- Runtime settings decoupled in `config/runtime_config.json`
- Evaluation artifacts in `evaluation/`
