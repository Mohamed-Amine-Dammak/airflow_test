name: DAG Promote
on: [workflow_dispatch]
jobs:
  promote:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python scripts/deployments/promote_candidate.py --root .
