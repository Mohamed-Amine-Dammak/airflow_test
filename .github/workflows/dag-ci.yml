name: dag-ci (legacy-manual)
on:
  workflow_dispatch:
jobs:
  dag-ci:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy workflow kept for manual use only. Use dag-ci-cd.yml for main flow."
